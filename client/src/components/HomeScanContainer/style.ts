import {makeStyles} from '@mui/styles';

export const styles = makeStyles({
  container: {
    marginTop: '15vh',
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
    alignContent: 'center',
    alignItems: 'center',
  },
});
